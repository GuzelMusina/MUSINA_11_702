подпются 2 строки очень длинные
умножение и сложение
в виде строк тк не помещаются в лонг
ф-я int[] sum(int a, int b){}
либо в виде строки. 


13.02.18
Коллекции АТД-что то куда можно положить и взять. Коллекция не гарантирует порядка, поэтому нельзя получить элемент по индексу. (Добавить, есть ли элемент, удалить, заменить)
Частный случай коллекции-список АТД. Гарантирует порядок.  (Можно пронумеровать элементы 0,1,2...н. Получить по индексу.) 
АТД не говорит как реализует. ТОлько говорит что как. Интерфейс короче. Мы не знаем как он реализуется
СД (массив) как мы реализуем. 
Реализовать можно список через массивы. 
Минус расширять тяжело, добавление в начало и в середину, но плюс есть доступ по индексу. Есть замена массиву - LinkedList.

Связанный список набор элементов каждый из которых указывает на следующий. Состоит из узлов. Рекурсивная структура. 

Для реализации нужен только один элемент

подпются 2 строки очень длинные
умножение и сложение
в виде строк тк не помещаются в лонг
ф-я int[] sum(int a, int b){}
либо в виде строки. 


13.02.18
Коллекции АТД-что то куда можно положить и взять. Коллекция не гарантирует порядка, поэтому нельзя получить элемент по индексу. (Добавить, есть ли элемент, удалить, заменить)
Частный случай коллекции-список АТД. Гарантирует порядок.  (Можно пронумеровать элементы 0,1,2...н. Получить по индексу.) 
АТД не говорит как реализует. ТОлько говорит что как. Интерфейс короче. Мы не знаем как он реализуется
СД (массив) как мы реализуем. 
Реализовать можно список через массивы. 
Минус расширять тяжело, добавление в начало и в середину, но плюс есть доступ по индексу. Есть замена массиву - LinkedList.

Связанный список набор элементов каждый из которых указывает на следующий. Состоит из узлов. Рекурсивная структура. 

Для реализации нужен только один элемент

Надо учиться сортировать список
стек массив списков 
ранг списка его длина


20.02.18
Коллекция
Можно класть любой тип.  

Обобщения <> Generics
T placeholder, мы говорим что это будет везде этот тип Т. Стирание типов, замена типа в <> на Object. Обобщенный метод. Две разные Т. В них разница есть. 1я параметризированный класс она подставляется в контейнер -> интерфейс и все методы его (во вторую) и в остальные методы.
Это подстановочные типы.

ДЗ.
Внедрить просто обобщения.  


22.02.18
Iterator
Iterable наша коллекция итерируема. 











22.02.18
"итератор" инетрфейс
Надо учиться сортировать список
стек массив списков 
ранг списка его длина


Волновой алгоритм, алгоритм Ли.
кратчайший путь на планарных графах (граф в виде сетки)


Hash это битовая строка. Набор цифр(символов). 

Рефлексия. Что это. 
Работа с классами джава. --classpath пути к нашим классам в качестве параметра передаются все классы, которые у нас есть. Если видит .class то сует его в classloader. Рефлексия позволяет работать так как будто класс она сама являетчя объектом. 
Class Field Constructor(с аргументами создавать обьекты) три класса, которые позволяет работаь с классом. 


